# --- data & outputs (never commit the dev object or generated packets) ---
data/
output/
figures/
sample_code/
*.rds
*.rda
*.feather
*.h5ad
*.h5seurat

# --- secrets / API keys ---
.Renviron
.env
*.key

# --- R session / project cruft ---
.Rhistory
.RData
.Rproj.user/
*.Rproj
.Ruserdata

# --- renv (if used) ---
renv/library/
renv/local/
renv/cellar/
renv/lock/
renv/staging/

# --- OS / cloud sync ---
.DS_Store
Icon?
*~
Thumbs.db

# --- misc ---
*.log
*.tmp
node_modules/

# --- internal dev docs (kept locally, not public) ---
CLAUDE.md
CLAUDE_old.md
STYLE.md
docs/
2026-*.md

# --- pkgdown output: built locally for preview, deployed via the
# --- pkgdown.yaml GitHub Action to the gh-pages branch, never committed
# --- to main (avoids leaking internal *.md pages and repo bloat) ---
pkgdown_site/

# --- Obsidian vault config/state, not project content ---
.obsidian/

# --- Claude Code local config (commands, settings), not public-facing ---
.claude/
