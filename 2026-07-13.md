**Issues regarding LLM usage limits 
- Serious issue with LLM APIs. They are super rate-limited for free versions (if they even exist at all). 
- Long term, I will have to shift towards an open-model solution. I can probably do this on my gaming PC since I have a GPU to use, but for now there's no chance and I will have to make do with the few Gemini free credits, or I'll have to shell out for some paid resources.
- For now I only have laptop access so I am stuck with free tiers etc for now. 
- We have to be very careful not to use too many requests etc, this should be made clear to claude moving forward.

This line of code works for using GitHub model marketplace, and this gives me 150 requests per day. 

`chat <- chat_github(model = "gpt-4o-mini")`

Anthropic gives 0 free requests per day, and Google gives 20. So I will have to tell Claude code to REALLY not send more requests than needed. For testing it should just send one module.

---
**Package name**
Regarding the package name, I already have "sentit" as an initial name but I am now thinking "prismatic" is better. The problem is that both prism and prismatic are popular R packages. Maybe like moduleprism, modprism, geneprism? modprism could be nice.

---
**Custom tools**
- One proof of concept custom tool could be a comparison with another set of signatures. For the CSF dataset, we can just compare with MSigDB, which is a general database of 50 gene signatures. We can first score the signatures with decoupleR and calculate correlations between the modules and the signatures. We can also perform a gene set overlap. 
	- Based on the current implementation, could these be two custom tools or just one?
- For SERPENTINE T-cell modules, we could definitely write a custom tool to relate this to T-cell clonality (would require pre-computed inputs since Gerard already calculated all of these stats)z.

---
**Outputs**
- Currently, do the tools produce tabular outputs?
	- Yes they do, they're under the `tables/` directory, however we should probably append the module name to each of the tables.
	- When we calculate UCell or DecoupleR scores, 

---
**Possible refactoring needed**
- We probably need to implement wrappers to UCell and decoupleR which can be used across several contexts. For instance, in the case where the user does not initially provide scores for their modules of interest, we need to calculate them. Additionally, for the module-signature correlation analysis, we need to calculate cell-level scores unless they are provided by the user.
	- Are there any custom inputs we should not allow the user to provide?
- How should we handle pseudo-bulk stuff? Perhaps we should use my SummarizedExperiment / MAE approach that we had with SERPENTINE? 
	- CSF isn't the best dataset to handle pseudo-bulk comparisons due to low sample size, a lot of different conditions, etc.

---
**Features to implement**
- User-provided inputs. For example, the user provides the DEG table so we don't need to do it ourselves. There will have to be a lot of considerations for how to handle the custom inputs. 
	- Do we have to enforce certain formats? Should we have helper functions to check for compatibility?
- We should work towards assembling a summary .html report!
	-  Should the report contain any plots? Maybe we can come up with a standard set of "EDA" plots to show, like the expression of each module in each cluster as a dotplot. Perhaps also the result of each tool can have (at least) one plot associated with it. For example, the DME test we can make a simple volcano plot? Gene overlap test we can make an overlap heatmap? These would be like summarizing over all modules, so I would need to ensure that none of these analysis are done one module at a time (not sure honestly).
	- Could we also provide some customization options to a report? For example, whether or not to plot. Maybe some custom plots could be included as well, like for hdWGCNA modules we could show the hub gene network plots.
- This would be WAY later down the road, but it would be super cool to "reconcile" summaries across different datasets. For example, in SERPENTINE we could run this pipeline to generate evidence packets on the SERPENTINE dataset, and then we run the pipeline again on TCGA bulk RNA-seq to get an additional set of evidence packets, and then finally we pass both packets together for interpretation. Maybe it would be a more niche use case for users in general but this would be useful to me in particular.

---
**Further open questions**
- I think it can be done just based on configuration but how should we handle bulk RNA-seq datasets? For example a bulk dataset wouldn't have a cluster column. 
- Thought: I think so far we do a good job of this, but this pipeline should be fully technology-agnostic within the realm of transcriptomics. We should be able to leverage this module interpretation pipeline on a bulk RNA-seq dataset + standard WGCNA, as well as a 10X scRNA-seq + hdWGCNA, as well as a 10X Xenium or Visium HD dataset with NMF, etc.