% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/agent_labels.R
\name{agent_label_backend}
\alias{agent_label_backend}
\title{Backend that replays agent-written label files}
\usage{
agent_label_backend(labels_dir, packets, agent = "coding-agent")
}
\arguments{
\item{labels_dir}{Directory holding one \verb{<module_id>.json} per module, each
filling the model-facing schema (\code{\link[=model_output_schema_json]{model_output_schema_json()}}).}

\item{packets}{The named list of evidence packets the labels describe.}

\item{agent}{Short name of the agent that wrote the labels, recorded on
each interpretation's provenance as \code{model = 'agent:<agent>'}.}
}
\value{
A backend function.
}
\description{
A synthesis backend (see \code{\link[=mock_backend]{mock_backend()}} for the contract) whose "model
output" is a JSON file an external coding agent wrote to
\verb{labels_dir/<module_id>.json} in a workspace exported with
\code{export_agent_workspace(mode = 'label')}. The packet a call is for is
resolved from \code{packet_hash}, so this plugs into \code{\link[=synthesize_module]{synthesize_module()}} and
\code{\link[=run_synthesis_orchestrator]{run_synthesis_orchestrator()}} unchanged.
}
\examples{
\dontrun{
backend <- agent_label_backend('agent_workspace/labels', packets, agent = 'claude-code')
synthesize_module(packets[[1]], desc, backend)
}
}
