% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats_utils.R
\name{aggregate_by_sample}
\alias{aggregate_by_sample}
\title{Aggregate module scores to the sample level}
\usage{
aggregate_by_sample(scores, sample_id, group_col = NULL)
}
\arguments{
\item{scores}{A numeric vector of per-cell module scores.}

\item{sample_id}{A grouping vector (sample id), same length as \code{scores}.}

\item{group_col}{Optional categorical vector (e.g. diagnosis), same length
as \code{scores}, assumed constant within each sample.}
}
\value{
A data.frame with one row per sample: \code{sample}, \code{mean_score}, and
\code{group} if \code{group_col} was supplied.
}
\description{
Mean module score per sample (the pseudoreplication fix: test at the
sample level, not the cell level), plus one label per sample for
\code{group_col} when supplied (assumes \code{group_col} is constant within sample).
}
