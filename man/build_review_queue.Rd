% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render.R
\name{build_review_queue}
\alias{build_review_queue}
\title{Build the review queue from a batch of interpretations}
\usage{
build_review_queue(interps)
}
\arguments{
\item{interps}{A named list of interpretations, e.g. the return value of
\code{\link[=run_synthesis_orchestrator]{run_synthesis_orchestrator()}}.}
}
\value{
A data.frame: \code{module_id}, \code{confidence}, \code{flags}, \code{reason}.
}
\description{
Only flagged interpretations (\code{\link[=needs_review]{needs_review()}}) -- this is a queue for a
human to triage, not a full summary of every module. Sorted
lowest-confidence first.
}
\examples{
ms <- llegir_example_moduleset()
packet <- run_module(ms, modules(ms)[1], list(list(fn = hub_genes_tool, params = list())))
desc <- dataset_description('human', 'CSF', 'myeloid', 'scRNA-seq')
interp <- synthesize_interpretation(packet, desc, mock_backend())
build_review_queue(list(m1 = interp))
}
