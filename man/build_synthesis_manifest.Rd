% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render.R
\name{build_synthesis_manifest}
\alias{build_synthesis_manifest}
\title{Build a run-level synthesis manifest}
\usage{
build_synthesis_manifest(
  interps,
  desc,
  prompt_template_version = PROMPT_TEMPLATE_VERSION,
  render_template_version = RENDER_TEMPLATE_VERSION
)
}
\arguments{
\item{interps}{A named list of interpretations; see \code{\link[=build_review_queue]{build_review_queue()}}.}

\item{desc}{The \code{dataset_description} used for this synthesis run.}

\item{prompt_template_version}{Prompt template version to record.}

\item{render_template_version}{Render template version to record.}
}
\value{
A list, suitable for \code{\link[=write_synthesis_manifest]{write_synthesis_manifest()}}.
}
\description{
Complements the per-interpretation provenance already attached by
\code{\link[=synthesize_interpretation]{synthesize_interpretation()}} with counts and the template versions used
to produce this batch of outputs.
}
\examples{
ms <- llegir_example_moduleset()
packet <- run_module(ms, modules(ms)[1], list(list(fn = top_genes_tool, params = list())))
desc <- dataset_description('human', 'CSF', 'myeloid', 'scRNA-seq')
interp <- synthesize_interpretation(packet, desc, mock_backend())
build_synthesis_manifest(list(m1 = interp), desc)
}
