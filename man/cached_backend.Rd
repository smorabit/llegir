% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synthesis.R
\name{cached_backend}
\alias{cached_backend}
\title{Cache a synthesis backend's raw model calls on disk}
\usage{
cached_backend(
  backend,
  provider,
  model,
  prompt_template_version,
  cache_dir = "output/cache",
  force_refresh = FALSE
)
}
\arguments{
\item{backend}{The backend function to wrap; see \code{\link[=mock_backend]{mock_backend()}} for the contract.}

\item{provider}{Provider name, used only as a cache-key component.}

\item{model}{Model id, used only as a cache-key component.}

\item{prompt_template_version}{Prompt template version, used only as a cache-key component.}

\item{cache_dir}{Directory to store cached \code{.rds} responses in.}

\item{force_refresh}{If \code{TRUE}, bypass the cache and always call \code{backend}.}
}
\value{
A backend function; see \code{\link[=mock_backend]{mock_backend()}} for the contract.
}
\description{
Wraps any backend with a cache keyed on \code{packet_hash} + \code{provider} +
\code{model} + \code{prompt_template_version}: on a hit, the inner backend (the live
API call) is skipped entirely. Deliberately caches only the raw model
call, not the full synthesized interpretation -- faithfulness, confidence
fusion, and rendering are cheap and deterministic and still re-run every
time on top of the cached content, so only an actual
packet/provider/model/prompt change ever costs an API call.
}
\examples{
backend <- cached_backend(mock_backend(), provider = 'mock', model = 'mock',
    prompt_template_version = '0.2', cache_dir = tempfile())
}
