% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/confidence.R
\name{calculate_fusion_score}
\alias{calculate_fusion_score}
\title{Calculate the deterministic fused evidence confidence for a packet}
\usage{
calculate_fusion_score(
  fragments,
  user_weights = list(),
  beta = 0.5,
  lambda = 0.35,
  kappa = 0.5
)
}
\arguments{
\item{fragments}{A list of \code{evidence_fragment} objects (a packet's \code{fragments}).}

\item{user_weights}{Named list of per-\code{tool_id} weight multipliers on top of
the structural tier base (see \code{\link[=register_tool]{register_tool()}}'s \code{tier} argument).
Default \code{list()} (all tiers unmodified).}

\item{beta}{Corroboration exponent for the power mean; lower is more
conjunctive (a single weak tool can't be offset by a strong one), higher
is more compensatory. Default \code{0.5}.}

\item{lambda}{Model-trust weight in the final geometric blend
(\code{\link[=fuse_confidence]{fuse_confidence()}}); threaded through only for provenance on the
returned list. Default \code{0.35}.}

\item{kappa}{Maximum directional-conflict penalty. Default \code{0.5}.}
}
\value{
A list: \code{e_evidence} (the final empirical evidence term),
\code{e_pool} (pooled evidence before the directional penalty), \code{p_agree}
(the directional penalty factor), \code{c_dir} (directional coherence),
\code{lambda}, \code{params} (\code{beta}, \code{kappa}), and \code{matrix} (a per-fragment
\code{data.frame} for prompt rendering and audit).
}
\description{
Normalizes every fragment onto a unified \verb{[0, 1]} scale
(\code{\link[=.fragment_score]{.fragment_score()}}: a type-aware magnitude link times a significance-
reliability factor), pools them with a weighted power mean
(\code{\link[=.pool_evidence]{.pool_evidence()}}) using tool-tier + \code{user_weights} importance
(\code{\link[=.tool_weight]{.tool_weight()}}), and applies a mass-weighted directional-conflict
penalty (\code{\link[=.directional_coherence]{.directional_coherence()}}). Computed straight from the packet
\emph{before} synthesis, so the result is reproducible from the packet hash
alone; it's injected into the synthesis prompt as ground truth
(\code{.render_confidence_matrix()}) and re-used by \code{\link[=fuse_confidence]{fuse_confidence()}} as the
evidence term, so the printed fusion string can never drift from the math.
}
\examples{
ms <- llegir_example_moduleset()
packet <- run_module(ms, modules(ms)[1], list(list(fn = top_genes_tool, params = list())))
calculate_fusion_score(packet$fragments)
}
