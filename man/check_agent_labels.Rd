% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/agent_labels.R
\name{check_agent_labels}
\alias{check_agent_labels}
\title{Check an agent's label files against their evidence packets}
\usage{
check_agent_labels(workspace_dir, alpha = 0.05)
}
\arguments{
\item{workspace_dir}{Path to the exported workspace.}

\item{alpha}{Significance threshold for the faithfulness wording check.}
}
\value{
A data.frame with one row per module (plus one per unexpected
label file): \code{module_id}, \code{status} (\code{'ok'}, \code{'missing'}, \code{'invalid'},
\code{'unfaithful'}, \code{'needs_revision'} or \code{'unexpected'}), \code{proposed_label}
and \code{detail}.
}
\description{
For every module in a workspace exported with
\code{export_agent_workspace(mode = 'label')}, replays \verb{labels/<module_id>.json}
through \code{\link[=synthesize_interpretation]{synthesize_interpretation()}} via \code{\link[=agent_label_backend]{agent_label_backend()}}, runs
\code{\link[=check_faithfulness]{check_faithfulness()}} against the module's packet, and checks the
required \code{review} block against the ModuleSet itself:
\itemize{
\item every supporting hub is among the module's top 25 hubs, with the kME
stated (within 0.01);
\item every expected marker's \code{present} call matches the module's membership;
\item the 3 display hubs are among the top 10 hubs, exclude housekeeping
normalizers, and do not repeat a gene family;
\item \code{confidence$score} lies in the band of \code{review$confidence_level}
(high 0.75-0.95, moderate 0.5-0.75, low 0.25-0.5, very_low 0-0.25);
\item a technical artifact gate, a \code{technical_artifact} evidence tier and the
\code{possible_artifact} flag go together;
\item working notes exist at \verb{labels/notes/<module_id>.md};
\item no two modules in the workspace share a \code{proposed_label}.
}
}
\details{
This is what the workspace's \code{validate_labels.R} runs, so an agent can
iterate until every module is \code{'ok'}.
}
