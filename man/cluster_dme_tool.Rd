% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tool_cluster_dme.R
\name{cluster_dme_tool}
\alias{cluster_dme_tool}
\title{Evidence tool: which cell states express this module}
\usage{
cluster_dme_tool(ctx)
}
\arguments{
\item{ctx}{A tool context list: \code{list(ms, module_id, params)}, as built by
\code{\link[=run_module]{run_module()}}. \code{ctx$params$group_by} (required) is the metadata column
naming the cell-state grouping (e.g. \code{'lv2_annot'}).}
}
\value{
An \code{evidence_fragment} of type \code{'state_expression'}, or \code{NULL} if
\code{ctx$ms} lacks the \code{clusters} or \code{module_scores} capability (see
\code{\link[=capabilities]{capabilities()}}) -- a graceful skip, not an error.
}
\description{
A core evidence tool. Touches only the \code{ModuleSet} adapter contract
(\code{\link[=module_scores]{module_scores()}}, \code{\link[=metadata]{metadata()}}, \code{\link[=pkg_versions]{pkg_versions()}}) plus the shared
\code{\link[=categorical_group_test]{categorical_group_test()}} helper, so it works against any backend.
}
\examples{
ms <- sentit_example_moduleset()
cluster_dme_tool(list(ms = ms, module_id = modules(ms)[1], params = list(group_by = 'cell_type')))
}
