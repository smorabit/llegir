% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tool_cluster_expression_profile.R
\name{cluster_expression_profile_tool}
\alias{cluster_expression_profile_tool}
\title{Evidence tool: where each cell state sits in the module's global score distribution}
\usage{
cluster_expression_profile_tool(ctx)
}
\arguments{
\item{ctx}{A tool context list: \code{list(ms, module_id, params)}, as built by
\code{\link[=run_module]{run_module()}}. \code{ctx$params$group_by} (required) is the metadata column
naming the cell-state grouping (e.g. \code{'cell_type'}). \code{ctx$params$n_deciles}
(default \code{10}) is the number of global bins requested; a module score
distribution with heavy ties can realize fewer unique bins than
requested, in which case the realized bin count is used throughout (see
Details).}
}
\value{
An \code{evidence_fragment} of type \code{'state_expression'}, or \code{NULL} if
\code{ctx$ms} lacks the \code{grouping} or \code{module_scores} capability (see
\code{\link[=capabilities]{capabilities()}}), or if the module's score distribution has no spread
at all (a single unique value, so no bin boundaries exist) -- both
graceful skips, not errors.
}
\description{
A core evidence tool, and the non-differential sibling of \code{\link[=cluster_dme_tool]{cluster_dme_tool()}}.
\code{cluster_dme_tool()} reports the single most enriched state from a one-vs-all
test, which can hide a state that expresses the module at a medium intensity.
This tool instead bins every cell's module score into deciles of the
module's own global score distribution (all cells in \code{ctx$ms}, not
re-binned per state) and reports, per state, the mean/median decile its
cells occupy and the share of its cells in the top bin -- a graded profile,
not a contrast, so it carries \code{direction = 'na'}. Touches only the
\code{ModuleSet} adapter contract (\code{\link[=module_scores]{module_scores()}}, \code{\link[=metadata]{metadata()}},
\code{\link[=pkg_versions]{pkg_versions()}}) plus base R quantile binning, so it works against any
backend.
}
\examples{
ms <- llegir_example_moduleset()
cluster_expression_profile_tool(list(
    ms = ms, module_id = modules(ms)[1], params = list(group_by = 'cell_type')
))
}
