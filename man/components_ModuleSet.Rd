% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/moduleset_components.R
\name{components_ModuleSet}
\alias{components_ModuleSet}
\alias{modules.components_ModuleSet}
\alias{gene_membership.components_ModuleSet}
\alias{module_scores.components_ModuleSet}
\alias{expression.components_ModuleSet}
\alias{metadata.components_ModuleSet}
\alias{pkg_versions.components_ModuleSet}
\alias{capabilities.components_ModuleSet}
\title{Build a ModuleSet from tidy components}
\usage{
components_ModuleSet(
  gene_table,
  expression,
  metadata,
  scores = NULL,
  cluster_col = NULL,
  sample_col = NULL
)

\method{modules}{components_ModuleSet}(ms, ...)

\method{gene_membership}{components_ModuleSet}(ms, module, ...)

\method{module_scores}{components_ModuleSet}(ms, module = NULL, ...)

\method{expression}{components_ModuleSet}(ms, ...)

\method{metadata}{components_ModuleSet}(ms, ...)

\method{pkg_versions}{components_ModuleSet}(ms, ...)

\method{capabilities}{components_ModuleSet}(ms, ...)
}
\arguments{
\item{gene_table}{A data.frame with one row per module-gene assignment:
\code{module}, \code{gene_name}, and an optional numeric \code{weight} column (e.g.
kME). If \code{weight} is absent, \code{\link[=gene_membership]{gene_membership()}} reports \code{kme = NA_real_} for every gene and \code{\link[=capabilities]{capabilities()}}\verb{$gene_weights} is \code{FALSE}.}

\item{expression}{A genes-by-cells (or genes-by-samples) numeric matrix.}

\item{metadata}{A data.frame with one row per cell/sample, aligned to the
columns of \code{expression}.}

\item{scores}{Optional module-scores data.frame/matrix: one row per
cell/sample (aligned to \code{expression}'s columns, same order), one column
per module -- the same shape \code{\link[=module_scores]{module_scores()}} documents. If omitted,
\code{\link[=module_scores]{module_scores()}} returns \code{NULL} and \code{\link[=capabilities]{capabilities()}}\verb{$module_scores} is
\code{FALSE}.}

\item{cluster_col}{Optional name of a \code{metadata} column declared as the
cell/sample-state grouping. This only drives
\code{\link[=capabilities]{capabilities()}}\verb{$clusters} -- core tools still take the grouping column
name as a parameter (e.g. \code{cluster_dme_tool}'s \code{group_by}); declaring
\code{cluster_col} is how this ModuleSet advertises that it supports the
concept at all.}

\item{sample_col}{Optional name of a \code{metadata} column declared as the
sample id, analogous to \code{cluster_col} for \code{\link[=capabilities]{capabilities()}}\verb{$sample_ids}.}

\item{ms}{A \code{ModuleSet} object; the dispatch target for the generic
methods below (\code{\link[=modules]{modules()}}, \code{\link[=gene_membership]{gene_membership()}}, \code{\link[=module_scores]{module_scores()}},
\code{\link[=expression]{expression()}}, \code{\link[=metadata]{metadata()}}, \code{\link[=pkg_versions]{pkg_versions()}}, \code{\link[=capabilities]{capabilities()}}).}

\item{...}{Passed to methods.}

\item{module}{A single module id, as returned by \code{\link[=modules]{modules()}}.}
}
\value{
A \code{components_ModuleSet} object.
}
\description{
The general-purpose \code{ModuleSet} substrate: takes a module<->gene table, an
optional module-scores matrix, an expression matrix, and metadata
directly, with no backend dependency. Other adapters (e.g.
\code{\link[=hdWGCNA_ModuleSet]{hdWGCNA_ModuleSet()}}, \code{\link[=gene_list_ModuleSet]{gene_list_ModuleSet()}}) build on this by
extracting their own data into these same shapes and delegating.
}
\examples{
gene_table <- data.frame(module = 'm1', gene_name = c('G1', 'G2'), weight = c(0.9, 0.5))
expr <- matrix(rnorm(20), nrow = 2, dimnames = list(c('G1', 'G2'), paste0('c', 1:10)))
meta <- data.frame(cell_type = rep(c('a', 'b'), 5), row.names = colnames(expr))
ms <- components_ModuleSet(gene_table, expr, meta, cluster_col = 'cell_type')
modules(ms)
}
