% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dataset_tools.R
\name{dataset_composition_tool}
\alias{dataset_composition_tool}
\title{Dataset tool: cell-state census and condition covariate balance}
\usage{
dataset_composition_tool(ctx)
}
\arguments{
\item{ctx}{A dataset tool context list: \code{list(ms, params, module_method)},
as built by \code{\link[=run_dataset_context]{run_dataset_context()}}. \code{ctx$params$group_col} (required) is
the metadata column naming the cell-state/cluster grouping.
\code{ctx$params$condition_col} (optional) is the metadata column to check the
grouping for skew against; when omitted, only a group census is
computed. \code{ctx$params$sample_col} (default \code{'sample'}) names the
metadata sample-id column, consulted when the \code{sample_ids} capability
holds. \code{ctx$params$residual_threshold} (default \code{2}) is the absolute
chi-square standardized residual above which a group x condition cell is
flagged. \code{ctx$params$min_samples} (default \code{3}) and
\code{ctx$params$min_cells} (default \code{20}) are the per-condition minimums
below which \code{'underpowered_contrast'} fires.}
}
\value{
A \code{dataset_fragment} of type \code{'composition_summary'}, or \code{NULL} if
\code{ctx$ms} lacks the \code{grouping} capability (see \code{\link[=capabilities]{capabilities()}}) -- a
graceful skip, not an error.
}
\description{
A core dataset tool. Touches only the \code{ModuleSet} adapter contract
(\code{\link[=metadata]{metadata()}}, \code{\link[=has_capability]{has_capability()}}, \code{\link[=pkg_versions]{pkg_versions()}}). Summarizes how units
(cells or samples) distribute across \code{ctx$params$group_col} and, when
\code{ctx$params$condition_col} is given, whether that distribution is skewed
across condition levels -- the compositional-confounding check every
per-module synthesis should see as global framing before it interprets a
module as biology rather than cell-type imbalance.
}
\examples{
ms <- llegir_example_moduleset()
params <- list(group_col = 'cell_type', condition_col = 'diagnosis')
dataset_composition_tool(list(ms = ms, params = params))
}
