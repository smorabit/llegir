% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dataset_fragment.R
\name{dataset_fragment}
\alias{dataset_fragment}
\title{Construct a dataset fragment}
\usage{
dataset_fragment(
  fragment_id,
  tool_id,
  type,
  result,
  compact_summary,
  top_findings,
  caveats = list(),
  provenance = list(),
  plots = NULL
)
}
\arguments{
\item{fragment_id}{Unique id within a dataset_context, e.g.
\code{'composition'} or \code{'milo::abundance'}.}

\item{tool_id}{Which tool produced this fragment.}

\item{type}{One of the controlled vocabulary: \code{'composition_summary'},
\code{'baseline_expression'}, \code{'variance_structure'}, \code{'module_landscape'}.}

\item{result}{The small tidy summary (a data.frame); never the raw matrix.}

\item{compact_summary}{Short digest for the model (token-efficient, no raw tables).}

\item{top_findings}{A list of the few most salient items (cell states / covariates / genes).}

\item{caveats}{A list of machine-readable confounder flags, drawn from the
controlled vocab. Default \code{list()}.}

\item{provenance}{A provenance list, typically built with \code{\link[=make_provenance]{make_provenance()}}.}

\item{plots}{Optional named list of plot specs to attach; see
\code{\link[=attach_plots]{attach_plots()}}. Default \code{NULL}.}
}
\value{
A \code{dataset_fragment} object.
}
\description{
One dataset-level tool's global summary of the whole experiment. Sibling
of \code{\link[=evidence_fragment]{evidence_fragment()}}: no \code{module_id}, no \code{effect_strength}/
\code{significance}/\code{direction} -- it is descriptive framing bundled into a
\code{dataset_context}, never fused or cited per module. See
\code{inst/schemas/dataset_fragment.schema.json} for the full contract.
}
