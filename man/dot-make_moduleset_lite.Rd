% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exporters.R
\name{.make_moduleset_lite}
\alias{.make_moduleset_lite}
\title{Build a reduced ModuleSet carrying only the token-safe views}
\usage{
.make_moduleset_lite(ms)
}
\arguments{
\item{ms}{A validated \code{ModuleSet}.}
}
\value{
A \code{components_ModuleSet} with \code{capabilities()$expression} and
\verb{$counts} \code{FALSE}.
}
\description{
Rebuilds a \code{components_ModuleSet} from \code{ms}'s own getters, carrying the
full gene-membership tables (every module), module scores, metadata, the
declared grouping/sample-id columns, \code{pkg_versions()}, \code{data_level}/
\code{aggregated}, and the pseudobulk view when one is attached and already
small -- but never the backing expression/counts matrices. This is the
artifact an agent workspace loads by default (\code{moduleset_lite.qs2}); the
full \code{ms} still ships alongside it (\code{moduleset_full.qs2}) for the rare
\code{expression()}/\code{counts()} query.
}
\details{
Reuses \code{\link[=components_ModuleSet]{components_ModuleSet()}} rather than a bespoke class so every
\code{ModuleSet} generic keeps dispatching unchanged against the reduced
object.
}
\keyword{internal}
