% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synthesis.R
\name{ellmer_backend}
\alias{ellmer_backend}
\title{Live synthesis backend via ellmer structured output}
\usage{
ellmer_backend(
  chat_fn = ellmer::chat_google_gemini,
  model = "gemini-3.5-flash",
  temperature = 0,
  credentials = NULL,
  schema_transform = identity
)
}
\arguments{
\item{chat_fn}{An \verb{ellmer::chat_*()} constructor, e.g.
\code{ellmer::chat_google_gemini} or \code{ellmer::chat_github}.}

\item{model}{Model id passed to \code{chat_fn}.}

\item{temperature}{Sampling temperature.}

\item{credentials}{Optional credentials passed to \code{chat_fn}; \code{NULL} uses
the provider's default environment-variable lookup.}

\item{schema_transform}{A \verb{function(schema_list) -> schema_list} applied
to the parsed canonical schema before it's sent to the provider.}
}
\value{
A backend function; see \code{\link[=mock_backend]{mock_backend()}} for the contract.
}
\description{
Not exercised by the offline test suite (needs network + a provider API
key). Provider and model are config-selected via \code{chat_fn}/\code{model};
credentials are picked up by \code{ellmer} itself from the provider's
environment variable (e.g. \code{GEMINI_API_KEY}, \code{GITHUB_PAT}) and are never
handled here. \code{schema_transform} adapts the one canonical schema to a
provider's structured-output dialect (e.g. the internal
\code{.to_openai_strict_schema()} used by \code{\link[=resolve_backend]{resolve_backend()}} for GitHub
Models) without forking the schema itself.
}
\examples{
\dontrun{
backend <- ellmer_backend(ellmer::chat_google_gemini, model = 'gemini-3.5-flash')
}
}
