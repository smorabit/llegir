% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tool_geneset_enrichment.R
\name{geneset_enrichment_tool}
\alias{geneset_enrichment_tool}
\title{Evidence tool: gene-set enrichment among a module's hub genes}
\usage{
geneset_enrichment_tool(ctx)
}
\arguments{
\item{ctx}{A tool context list: \code{list(ms, module_id, params)}, as built by
\code{\link[=run_module]{run_module()}}. \code{ctx$params$n_hubs} (default 25) is the number of hub
genes tested. \code{ctx$params$db_files} (required for a non-empty result) is
a named character vector of local GMT file paths, e.g.
\code{c(GO_BP = 'path/to/GO_Biological_Process.txt')}.}
}
\value{
An \code{evidence_fragment} of type \code{'geneset_enrichment'}, or \code{NULL} if
\code{ctx$ms} lacks the \code{expression} capability (see \code{\link[=capabilities]{capabilities()}}) -- a
graceful skip, not an error.
}
\description{
Offline GO/pathway enrichment (\code{\link[GeneOverlap:newGOM]{GeneOverlap::newGOM()}}) over hub genes
against local GMT gene-set libraries (\code{\link[fgsea:gmtPathways]{fgsea::gmtPathways()}}) -- no
runtime network access, so the tool is deterministic and CI-clean by
construction. Touches the \code{ModuleSet} adapter contract
(\code{\link[=gene_membership]{gene_membership()}}, \code{\link[=expression]{expression()}}, \code{\link[=pkg_versions]{pkg_versions()}}) plus
\code{ctx$params$db_files}, a named vector of local GMT file paths.
}
\examples{
\dontrun{
ms <- sentit_example_moduleset()
geneset_enrichment_tool(list(
    ms = ms, module_id = modules(ms)[1],
    params = list(n_hubs = 10, db_files = c(GO_BP = 'path/to/gene_sets.gmt'))
))
}
}
