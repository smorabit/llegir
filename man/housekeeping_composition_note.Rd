% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/confidence.R
\name{housekeeping_composition_note}
\alias{housekeeping_composition_note}
\title{Housekeeping-composition note for a packet's hub-gene list}
\usage{
housekeeping_composition_note(packet, top_n = 20, min_fraction = 0.15)
}
\arguments{
\item{packet}{An evidence packet, as built by \code{\link[=build_evidence_packet]{build_evidence_packet()}}.}

\item{top_n}{Number of top hub genes to inspect. Default \code{20}.}

\item{min_fraction}{Fraction a family must reach to be worth noting.
Default \code{0.15}.}
}
\value{
A single-string note, or \code{NULL} when neither family clears
\code{min_fraction} or the packet carries no \code{ranked_genes} fragment.
}
\description{
The fraction of a module's top hub genes that are cytoplasmic ribosomal
(\verb{RPS*}/\verb{RPL*}) or mitochondrially encoded (\verb{MT-*}). Surfaced to synthesis
via \code{\link[=render_packet_compact]{render_packet_compact()}} as a short note appended to the
\code{ranked_genes} fragment, so a module whose hub list is part housekeeping
reads as "\if{html}{\out{<program>}} AND ribosomal-high" instead of having the housekeeping
genes quietly dilute the biological call. Deterministic, packet-only, and
-- unlike \code{.artifact_flagged()}'s IEG pattern -- never sets a flag on its own.
}
\examples{
ms <- llegir_example_moduleset()
packet <- run_module(ms, modules(ms)[1], list(list(fn = top_genes_tool, params = list())))
housekeeping_composition_note(packet)
}
