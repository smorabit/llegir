% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import_fragment.R
\name{import_hdwgcna_dme}
\alias{import_hdwgcna_dme}
\title{Import an hdWGCNA differential module eigengene (DME) table}
\usage{
import_hdwgcna_dme(
  module_id,
  result,
  column_map = list(),
  source_file = NULL,
  ...
)
}
\arguments{
\item{module_id}{The module this fragment describes.}

\item{result}{A tidy DME result data.frame (one row per group).}

\item{column_map}{Named list of column overrides: \code{group_col} (default
\code{'group'}), \code{effect_col} (default \code{'avg_log2FC'}), \code{significance_col}
(default \code{'p_val_adj'}).}

\item{source_file}{Optional path \code{result} was read from; see
\code{\link[=import_fragment]{import_fragment()}}.}

\item{...}{Passed to \code{\link[=import_fragment]{import_fragment()}} (e.g. \code{fragment_id}, \code{tool_id}).}
}
\value{
An \code{evidence_fragment} object.
}
\description{
Sensible defaults for \code{hdWGCNA::FindAllDMEs()} output (\code{group},
\code{avg_log2FC}, \code{p_val_adj}). Produces a \code{'state_expression'} fragment — the
same type \code{\link[=cluster_dme_tool]{cluster_dme_tool()}} produces — so an externally computed DME
table (run outside this package, or against a different grouping) feeds
the synthesis layer identically to the built-in tool.
}
