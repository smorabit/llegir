% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import_fragment.R
\name{import_milo_da}
\alias{import_milo_da}
\title{Import a miloR differential-abundance neighborhood table}
\usage{
import_milo_da(
  result,
  column_map = list(),
  alpha = 0.05,
  source_file = NULL,
  ...
)
}
\arguments{
\item{result}{A tidy miloR DA neighborhood result data.frame (one row per
neighborhood).}

\item{column_map}{Named list of column overrides: \code{group_col} (default
\code{'celltype'}), \code{effect_col} (default \code{'logFC'}), \code{significance_col}
(default \code{'SpatialFDR'}).}

\item{alpha}{Significance threshold for a neighborhood to count as
shifted. Default \code{0.05}.}

\item{source_file}{Optional path \code{result} was read from; see
\code{\link[=import_dataset_fragment]{import_dataset_fragment()}}.}

\item{...}{Passed to \code{\link[=import_dataset_fragment]{import_dataset_fragment()}} (e.g. \code{fragment_id}, \code{tool_id}).}
}
\value{
A \code{dataset_fragment} object.
}
\description{
Sensible defaults for \code{miloR::testNhoods()} output merged with
\code{miloR::annotateNhoods()} (\code{logFC}, \code{SpatialFDR}, and a majority-vote
cell-type annotation column defaulting to \code{celltype}). Neighborhoods are
aggregated per annotation group into a \code{'composition_summary'}
\code{\link[=dataset_fragment]{dataset_fragment()}}: how many neighborhoods per cell state shift
significantly and in which direction -- never the raw per-neighborhood
table.
}
