% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import_fragment.R
\name{import_seurat_markers}
\alias{import_seurat_markers}
\title{Import a Seurat / DESeq2 / edgeR differential-expression table}
\usage{
import_seurat_markers(
  module_id,
  result,
  group_col = "cluster",
  column_map = list(),
  source_file = NULL,
  ...
)
}
\arguments{
\item{module_id}{The module this fragment describes.}

\item{result}{A tidy DE result data.frame.}

\item{group_col}{Column naming a group/cluster contrast, if any (default
\code{'cluster'}). Only used if present in \code{result}.}

\item{column_map}{Named list of column overrides: \code{feature_col} (default
\code{'gene'}, falls back to rownames if absent), \code{effect_col} (default
\code{'avg_log2FC'}), \code{significance_col} (default \code{'p_val_adj'}).}

\item{source_file}{Optional path \code{result} was read from; see
\code{\link[=import_fragment]{import_fragment()}}.}

\item{...}{Passed to \code{\link[=import_fragment]{import_fragment()}} (e.g. \code{fragment_id}, \code{tool_id}).}
}
\value{
An \code{evidence_fragment} object.
}
\description{
Sensible defaults for \code{Seurat::FindMarkers()} output (\code{avg_log2FC},
\code{p_val_adj}, feature names as rownames). Point \code{column_map} at a DESeq2
(\code{log2FoldChange}/\code{padj}) or edgeR (\code{logFC}/\code{FDR}) table instead to reuse
the same importer. If \code{result} has a \code{group_col} (e.g. \code{cluster}, as in
\code{Seurat::FindAllMarkers()}), the fragment is a per-group
\code{'categorical_association'}; otherwise it's a single-contrast
\code{'cross_condition_delta'} (one row per gene/feature).
}
