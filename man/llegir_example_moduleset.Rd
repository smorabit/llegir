% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/example_moduleset.R
\name{llegir_example_moduleset}
\alias{llegir_example_moduleset}
\title{A small, self-contained synthetic ModuleSet for examples and the vignette}
\usage{
llegir_example_moduleset(seed = 1)
}
\arguments{
\item{seed}{Random seed for reproducibility. Default \code{1}.}
}
\value{
A \code{synthetic_ModuleSet} object with two modules, \code{'module_a'} and
\code{'module_b'}.
}
\description{
Builds a tiny simulated single-cell-like dataset (no Seurat/hdWGCNA
involved, no external data file) with two co-expressed gene modules --
\code{'module_a'}, associated with a simulated \code{diagnosis} column, and
\code{'module_b'}, associated with a simulated \code{cell_type} column -- plus
background noise genes, and wraps it as a \code{ModuleSet} via
\code{\link[=synthetic_ModuleSet]{synthetic_ModuleSet()}}. Runs fully offline and deterministically (fixed
seed), so it is safe to use in \verb{@examples}, tests, and the package
vignette without any real dataset.
}
\details{
The simulated \code{\link[=metadata]{metadata()}} includes \code{diagnosis}, \code{cell_type}, and \code{sample}
columns, matching what \code{\link[=module_by_metadata_tool]{module_by_metadata_tool()}} and
\code{\link[=cluster_dme_tool]{cluster_dme_tool()}} expect.
}
\examples{
ms <- llegir_example_moduleset()
modules(ms)
gene_membership(ms, 'module_a')
}
