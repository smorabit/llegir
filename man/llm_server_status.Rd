% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synthesis.R
\name{llm_server_status}
\alias{llm_server_status}
\title{Query a local vLLM server's model list}
\usage{
llm_server_status(
  base_url = Sys.getenv("LLEGIR_LLM_URL", "http://127.0.0.1:8000/v1")
)
}
\arguments{
\item{base_url}{Base URL of the OpenAI-compatible server (no trailing
slash); defaults to \code{LLEGIR_LLM_URL} or \verb{http://127.0.0.1:8000/v1}.}
}
\value{
A list with two elements: \code{base_url} (character) and \code{models}
(character vector of served model ids as reported by the \verb{/models} endpoint).
}
\description{
GETs \verb{<base_url>/models} and returns the base URL and the served model ids.
Used by \code{\link[=resolve_backend]{resolve_backend()}} for model auto-discovery and by \code{\link[=local_backend]{local_backend()}}
for connectivity pre-flight. Throws a clear, actionable error if the server
is unreachable so the failure site is visible at the top of the call stack
rather than buried in an httr2 connection error.
}
\examples{
\dontrun{
status <- llm_server_status()
cat('Served models:', paste(status$models, collapse = ', '), '\n')
}
}
