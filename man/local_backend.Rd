% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synthesis.R
\name{local_backend}
\alias{local_backend}
\title{Convenience constructor for the local vLLM backend}
\usage{
local_backend(
  model = NULL,
  cache = TRUE,
  force_refresh = FALSE,
  prompt_template_version = PROMPT_TEMPLATE_VERSION
)
}
\arguments{
\item{model}{Model id served by the vLLM server. When \code{NULL} (default),
the server's model list is queried via \code{\link[=llm_server_status]{llm_server_status()}}; a hard
error is raised if zero or more than one models are served so the
correct id can be specified explicitly.}

\item{cache}{If \code{TRUE} (default), wraps the live backend with
\code{\link[=cached_backend]{cached_backend()}} keyed on provider, model, and prompt template version.}

\item{force_refresh}{Passed to \code{\link[=cached_backend]{cached_backend()}}; if \code{TRUE}, bypasses any
cached response and always calls the live server.}

\item{prompt_template_version}{Cache-key component passed to
\code{\link[=cached_backend]{cached_backend()}}; defaults to the package-level \link{PROMPT_TEMPLATE_VERSION}.}
}
\value{
A backend function; see \code{\link[=mock_backend]{mock_backend()}} for the contract.
}
\description{
Combines connectivity pre-flight (\code{\link[=llm_server_status]{llm_server_status()}}), model
auto-discovery, \code{\link[=resolve_backend]{resolve_backend()}} for \code{'local'}, and \code{\link[=cached_backend]{cached_backend()}}
into a single call. Returns a ready-to-use cached backend suitable for
\code{\link[=synthesize_interpretation]{synthesize_interpretation()}} and \code{\link[=run_synthesis_orchestrator]{run_synthesis_orchestrator()}}.
}
\examples{
\dontrun{
backend <- local_backend()
}
}
