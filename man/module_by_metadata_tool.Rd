% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tool_module_by_metadata.R
\name{module_by_metadata_tool}
\alias{module_by_metadata_tool}
\title{Evidence tool: module score vs. a declared metadata column}
\usage{
module_by_metadata_tool(ctx)
}
\arguments{
\item{ctx}{A tool context list: \code{list(ms, module_id, params)}, as built by
\code{\link[=run_module]{run_module()}}. \code{ctx$params$column} (required) is the metadata column to
test. \code{ctx$params$column_type} is \code{'categorical'} (default) or
\code{'continuous'}. \code{ctx$params$sample_col} (default \code{'sample'}) is the
sample-id column used for the pseudoreplication fix. \code{ctx$params$level}
is \code{'auto'} (default), \code{'cell'}, or \code{'sample'}.}
}
\value{
An \code{evidence_fragment} of type \code{'categorical_association'} or
\code{'continuous_correlation'}, or \code{NULL} if \code{ctx$ms} lacks a capability this
call needs (\code{module_scores} always; \code{sample_ids} for categorical
testing, see \code{\link[=capabilities]{capabilities()}}) -- a graceful skip, not an error.
}
\description{
A core evidence tool. Touches only the \code{ModuleSet} adapter contract
(\code{\link[=module_scores]{module_scores()}}, \code{\link[=metadata]{metadata()}}, \code{\link[=pkg_versions]{pkg_versions()}}) plus the shared
\code{\link[=categorical_group_test]{categorical_group_test()}} / \code{\link[=continuous_correlation_test]{continuous_correlation_test()}} helpers.
Categorical columns (e.g. diagnosis, sample) get group means plus
Kruskal/one-vs-rest Wilcoxon; continuous columns get Pearson/Spearman
correlation.
}
\examples{
ms <- sentit_example_moduleset()
module_by_metadata_tool(list(
    ms = ms, module_id = modules(ms)[1],
    params = list(column = 'diagnosis', column_type = 'categorical')
))
}
