% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/moduleset_pseudobulk.R
\name{pseudobulk_ModuleSet}
\alias{pseudobulk_ModuleSet}
\alias{pkg_versions.pseudobulk_ModuleSet}
\title{Build a ModuleSet from pseudo-bulk counts, re-scoring modules with decoupleR}
\usage{
pseudobulk_ModuleSet(
  counts,
  gene_table,
  metadata = NULL,
  assay = "counts",
  group_col = NULL,
  sample_col = NULL,
  data_level = "pseudobulk"
)

\method{pkg_versions}{pseudobulk_ModuleSet}(ms, ...)
}
\arguments{
\item{counts}{Either a raw-counts matrix (genes x pseudo-bulk units) or a
\code{SummarizedExperiment} (assay \code{assay}, \code{colData} becomes \code{metadata},
\code{rownames} are genes).}

\item{gene_table}{The module definitions to score: a tidy data.frame with
\code{module}, \code{gene_name}, and an optional numeric \code{weight} column, or a
named list of gene vectors (one per module, no weights). Normally the
same modules found at cell level.}

\item{metadata}{A data.frame with one row per pseudo-bulk unit, aligned to
the columns of \code{counts}. Required when \code{counts} is a raw matrix; ignored
(taken from \code{colData}) when \code{counts} is a \code{SummarizedExperiment}.}

\item{assay}{Assay name to read counts from when \code{counts} is a
\code{SummarizedExperiment}. Default \code{'counts'}.}

\item{group_col}{Optional name of a \code{metadata} column declared as the
unit-state grouping (see \code{\link[=components_ModuleSet]{components_ModuleSet()}}).}

\item{sample_col}{Optional name of a \code{metadata} column declared as the
sample id.}

\item{data_level}{Observation-unit descriptor. Default \code{'pseudobulk'};
override for finer-grained units, e.g. \code{'pseudobulk_sample_x_cluster'}.}

\item{ms}{A \code{ModuleSet} object; the dispatch target for \code{\link[=pkg_versions]{pkg_versions()}}.}

\item{...}{Passed to methods.}
}
\value{
A \code{pseudobulk_ModuleSet} object (a \code{components_ModuleSet} under
the hood).
}
\description{
Ingests a user-supplied pseudo-bulk counts matrix -- llegir does not build
pseudo-bulk itself -- and re-scores the module definitions directly on it,
rather than averaging cell-level module scores. Scoring runs on a
log2-CPM normalization of \code{counts} via \code{\link[decoupleR:run_ulm]{decoupleR::run_ulm()}}: each
gene's \code{weight} in \code{gene_table} (e.g. hdWGCNA kME) becomes decoupleR's
mode-of-regulation (\code{mor}) when present, otherwise every gene gets a
uniform \code{mor = 1}. Builds on \code{\link[=components_ModuleSet]{components_ModuleSet()}} with \code{data_level = 'pseudobulk'} and \code{aggregated = TRUE}, so \code{\link[=validate_moduleset]{validate_moduleset()}} passes
and \code{counts()}/\code{capabilities()$counts} are populated.
}
\examples{
\dontrun{
pb_ms <- pseudobulk_ModuleSet(pb_counts, list(module_a = c('GENE1', 'GENE2')), pb_meta)
validate_moduleset(pb_ms)
}
}
