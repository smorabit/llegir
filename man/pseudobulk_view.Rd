% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/moduleset_pseudobulk.R
\name{pseudobulk_view}
\alias{pseudobulk_view}
\title{Resolve the pseudo-bulk view for a ModuleSet}
\usage{
pseudobulk_view(ms)
}
\arguments{
\item{ms}{A \code{ModuleSet} object.}
}
\value{
A \code{ModuleSet}, or \code{NULL}.
}
\description{
The resolver every pseudo-bulk tool uses: returns \code{ms} itself when it is
already a pseudo-bulk set (built by \code{\link[=pseudobulk_ModuleSet]{pseudobulk_ModuleSet()}}), returns the
attached view via \code{\link[=pseudobulk]{pseudobulk()}} when one exists, and \code{NULL} otherwise --
the signal for a tool to skip gracefully.
}
