% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{register_tool}
\alias{register_tool}
\title{Register a tool (core or custom) in the tool registry}
\usage{
register_tool(id, fn, type, description, requires = character(0))
}
\arguments{
\item{id}{A unique tool id, e.g. \code{'hub_genes'} or \code{'my_custom_tool'}.}

\item{fn}{A \verb{function(ctx) -> evidence_fragment}, where \code{ctx} is
\code{list(ms, module_id, params)} (see \code{\link[=run_module]{run_module()}}).}

\item{type}{One or more of the \code{evidence_fragment} controlled vocabulary
(see \code{\link[=evidence_fragment]{evidence_fragment()}}) this tool may emit. Descriptive only -- the
fragment a tool actually returns is always checked against the full
contract by \code{\link[=validate_evidence_fragment]{validate_evidence_fragment()}} regardless of what's declared
here. A tool whose emitted type depends on its params (e.g.
\code{\link[=module_by_metadata_tool]{module_by_metadata_tool()}}) can declare more than one.}

\item{description}{A one-line, human-readable description of what the tool does.}

\item{requires}{The \code{ModuleSet} \code{\link[=capabilities]{capabilities()}} this tool needs to run:
either a character vector (e.g. \code{c('clusters', 'module_scores')}), or a
\verb{function(params) -> character vector} for a tool whose requirement
depends on how it's called (e.g. \code{\link[=module_by_metadata_tool]{module_by_metadata_tool()}} only needs
\code{sample_ids} for categorical columns). Default \code{character(0)} (no
requirement).}
}
\value{
\code{id}, invisibly.
}
\description{
A tool is any \verb{function(ctx) -> evidence_fragment} (or \code{NULL}, to skip;
see \code{\link[=run_module]{run_module()}}). Registering it makes it runnable from \code{tool_config}
by id (\code{list(id = 'my_tool', params = list(...))}) and lets
\code{\link[=run_module]{run_module()}} check its required \code{ModuleSet} \code{\link[=capabilities]{capabilities()}} before
calling it, skipping gracefully and recording why in the packet if
they're unmet, rather than the tool having to self-skip. See
\code{docs/custom_tools.md} for a worked template.
}
\examples{
my_tool <- function(ctx) hub_genes_tool(ctx)
register_tool(
    'my_tool', my_tool, type = 'ranked_genes',
    description = 'demo', requires = character(0)
)
}
