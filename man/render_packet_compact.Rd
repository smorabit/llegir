% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prompt.R
\name{render_packet_compact}
\alias{render_packet_compact}
\title{Render an evidence packet as a compact model-facing text block}
\usage{
render_packet_compact(packet, max_findings = 8)
}
\arguments{
\item{packet}{An evidence packet, as built by \code{\link[=build_evidence_packet]{build_evidence_packet()}}.}

\item{max_findings}{Maximum number of \code{top_findings} entries rendered per
fragment; a token-efficiency backstop, since tools already curate
\code{top_findings} to a handful of entries.}
}
\value{
A single character string.
}
\description{
Renders only the compact, curated fields of each fragment
(\code{compact_summary}, \code{top_findings}, \code{effect_strength}/\code{significance}) --
never the raw \code{result} tables -- so the prompt stays small and the model
only sees pre-summarized evidence.
}
\examples{
ms <- llegir_example_moduleset()
packet <- run_module(ms, modules(ms)[1], list(list(fn = top_genes_tool, params = list())))
cat(render_packet_compact(packet))
}
