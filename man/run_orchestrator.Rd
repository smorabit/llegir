% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/orchestrator.R
\name{run_orchestrator}
\alias{run_orchestrator}
\title{Run a configured set of evidence tools over every module in a ModuleSet}
\usage{
run_orchestrator(
  ms,
  tool_config,
  output_dir,
  tables_dir = NULL,
  modules_use = NULL,
  input_hash = NA_character_,
  module_method = NA_character_,
  validate = TRUE
)
}
\arguments{
\item{ms}{A \code{ModuleSet}.}

\item{tool_config}{A list of \code{list(fn, params)} specs; see \code{\link[=run_module]{run_module()}}.}

\item{output_dir}{Directory to write one evidence packet JSON file per module.}

\item{tables_dir}{Optional directory to also persist every fragment's
result table.}

\item{modules_use}{Optional subset of module ids to run; defaults to all
modules in \code{ms}.}

\item{input_hash}{Optional hash of the input \code{ModuleSet}, recorded on each packet.}

\item{module_method}{Optional free-form description of how the modules
themselves were generated; see \code{\link[=run_module]{run_module()}} / \code{\link[=dataset_description]{dataset_description()}}.
Passed through to every \code{\link[=run_module]{run_module()}} call. Default \code{NA}.}

\item{validate}{If \code{TRUE} (default), run \code{\link[=validate_moduleset]{validate_moduleset()}} on \code{ms}
before doing anything else, so a malformed adapter fails loudly up
front instead of partway through a batch.}
}
\value{
Invisibly, a named list of evidence packets (one per module; \code{NULL}
for any module that failed).
}
\description{
Runs \code{\link[=run_module]{run_module()}} independently for each module in \code{modules_use} (or
every module in \code{ms} if not given) and writes each validated, hashed
evidence packet to \code{output_dir} as JSON. One module's failure (e.g. a tool
erroring on a degenerate module) doesn't take down the whole batch;
failures are reported via \code{warning()} and recorded as \code{NULL} in the
returned list. \code{tables_dir}, when given, also persists every fragment's
result table (via \code{\link[=write_fragment_tables]{write_fragment_tables()}}) alongside the JSON packets.
}
\examples{
ms <- llegir_example_moduleset()
run_orchestrator(ms, list(list(fn = top_genes_tool, params = list())), output_dir = tempfile())
}
