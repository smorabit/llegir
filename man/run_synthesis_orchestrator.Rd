% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/orchestrator.R
\name{run_synthesis_orchestrator}
\alias{run_synthesis_orchestrator}
\title{Run synthesis over a batch of evidence packets}
\usage{
run_synthesis_orchestrator(
  packets,
  desc,
  backend,
  output_dir,
  temperature = 0,
  seed = NA_real_,
  prompt_template_version = PROMPT_TEMPLATE_VERSION,
  schema_path = system.file("schemas", "interpretation.schema.json", package = "sentit")
)
}
\arguments{
\item{packets}{A named list of evidence packets, e.g. the return value of
\code{\link[=run_orchestrator]{run_orchestrator()}}.}

\item{desc}{A \code{dataset_description}; see \code{\link[=dataset_description]{dataset_description()}}.}

\item{backend}{A synthesis backend function; see \code{\link[=mock_backend]{mock_backend()}},
\code{\link[=ellmer_backend]{ellmer_backend()}}, \code{\link[=resolve_backend]{resolve_backend()}}.}

\item{output_dir}{Directory to write per-module interpretations,
paragraphs, \code{review_queue.tsv}, and \code{manifest.json}.}

\item{temperature}{Sampling temperature passed to the backend.}

\item{seed}{Optional seed, recorded on each interpretation's provenance.}

\item{prompt_template_version}{Prompt template version to record on each
interpretation's provenance.}

\item{schema_path}{Path to the interpretation JSON schema; defaults to the
schema shipped with the package.}
}
\value{
Invisibly, a named list of interpretation objects (one per module;
\code{NULL} for any module whose synthesis failed).
}
\description{
Runs \code{\link[=synthesize_module]{synthesize_module()}} independently for each packet in \code{packets},
writing per-module interpretation JSON and rendered Markdown paragraphs
(via \code{\link[=render_paragraph]{render_paragraph()}}) to \code{output_dir}, plus a run-level
\code{review_queue.tsv} (\code{\link[=write_review_queue]{write_review_queue()}}) and \code{manifest.json}
(\code{\link[=write_synthesis_manifest]{write_synthesis_manifest()}}). One module's synthesis failure is warned
and recorded as \code{NULL}, mirroring \code{\link[=run_orchestrator]{run_orchestrator()}}'s failure isolation.
}
\examples{
ms <- sentit_example_moduleset()
packets <- run_orchestrator(ms, list(list(fn = hub_genes_tool, params = list())), output_dir = tempfile())
desc <- dataset_description('human', 'CSF', 'myeloid', 'scRNA-seq')
run_synthesis_orchestrator(packets, desc, mock_backend(), output_dir = tempfile())
}
