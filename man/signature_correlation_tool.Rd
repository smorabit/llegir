% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tool_signature_correlation.R
\name{signature_correlation_tool}
\alias{signature_correlation_tool}
\title{Evidence tool: correlate module activity with a signature library}
\usage{
signature_correlation_tool(ctx)
}
\arguments{
\item{ctx}{A tool context list: \code{list(ms, module_id, params)}, as built by
\code{\link[=run_module]{run_module()}}. \code{ctx$params$library_files} (required) is a named
character vector of local \code{.gmt} or \code{.rds} signature-library file paths,
e.g. \code{c(Hallmark = 'data/h.all.v2026.1.Hs.symbols.gmt')}. An \code{.rds} file
must contain a named list of character vectors (gene sets); a \code{.gmt} is
read via \code{\link[fgsea:gmtPathways]{fgsea::gmtPathways()}}. \code{ctx$params$method} is \code{'UCell'}
(default) or \code{'decoupleR'}.}
}
\value{
An \code{evidence_fragment} of type \code{'signature_correlation'}, or
\code{NULL} if \code{ctx$ms} lacks the \code{module_scores} or \code{expression} capability
(see \code{\link[=capabilities]{capabilities()}}) -- a graceful skip, not an error.
}
\description{
The co-variation sibling of \code{\link[=geneset_enrichment_tool]{geneset_enrichment_tool()}}: overlap asks
whether a module \emph{contains} a signature's genes; this tool asks whether
the module's \emph{activity} co-varies with the signature's. Scores every
signature in \code{ctx$params$library_files} across cells via
\code{\link[UCell:ScoreSignatures_UCell]{UCell::ScoreSignatures_UCell()}} or \code{\link[decoupleR:run_ulm]{decoupleR::run_ulm()}} (the same
scoring \code{\link[=gene_list_ModuleSet]{gene_list_ModuleSet()}} uses), then correlates each signature's
score with this module's \code{\link[=module_scores]{module_scores()}}.
}
\details{
Reports Pearson \emph{r} as descriptive co-variation at the cell level always
(never a p-value there -- cells aren't independent). When
\code{\link[=pseudobulk_view]{pseudobulk_view()}} resolves a pseudo-bulk view for \code{ctx$ms} (either
\code{ctx$ms} is itself a pseudo-bulk \code{ModuleSet}, or one is attached via
\code{\link[=with_pseudobulk]{with_pseudobulk()}}), the signature library is re-scored on that view's
own expression and correlated against its own \code{\link[=module_scores]{module_scores()}} instead,
with a real p-value from those independent pseudo-bulk units. With no
pseudo-bulk view available, only the descriptive cell-level \emph{r} is
reported.
}
\examples{
\dontrun{
ms <- llegir_example_moduleset()
signature_correlation_tool(list(
    ms = ms, module_id = modules(ms)[1],
    params = list(library_files = c(Hallmark = 'data/h.all.v2026.1.Hs.symbols.gmt'))
))
}
}
