% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synthesis.R
\name{synthesize_interpretation}
\alias{synthesize_interpretation}
\title{Synthesize one evidence packet into an interpretation via a backend}
\usage{
synthesize_interpretation(
  packet,
  desc,
  backend,
  temperature = 0,
  seed = NA_real_,
  prompt_template_version = PROMPT_TEMPLATE_VERSION,
  schema_path = system.file("schemas", "interpretation.schema.json", package = "sentit")
)
}
\arguments{
\item{packet}{An evidence packet, as built by \code{\link[=build_evidence_packet]{build_evidence_packet()}}.}

\item{desc}{A \code{dataset_description}; see \code{\link[=dataset_description]{dataset_description()}}. Required
(hard error via \code{\link[=validate_dataset_description]{validate_dataset_description()}} if missing/empty).}

\item{backend}{A backend function; see \code{\link[=mock_backend]{mock_backend()}} for the contract.}

\item{temperature}{Sampling temperature passed to the backend.}

\item{seed}{Optional seed, recorded on the interpretation's provenance.}

\item{prompt_template_version}{Prompt template version to record on the
interpretation's provenance.}

\item{schema_path}{Path to the interpretation JSON schema; defaults to the
schema shipped with the package.}
}
\value{
An \code{interpretation} object (not yet faithfulness-checked or
confidence-fused; see \code{\link[=synthesize_module]{synthesize_module()}} for the full pipeline).
}
\description{
The model's role is bounded: it fills the model-facing schema
(\code{\link[=model_output_schema_json]{model_output_schema_json()}}) from the compact prompt built by
\code{\link[=build_system_prompt]{build_system_prompt()}} / \code{\link[=build_user_prompt]{build_user_prompt()}}; it never sees the raw
evidence result tables and never runs analysis code. \code{module_id} is always
taken from \code{packet}, never trusted from the model's output, and
\code{literature} is always forced empty (out of scope for this milestone
regardless of what a backend returns).
}
\examples{
ms <- sentit_example_moduleset()
packet <- run_module(ms, modules(ms)[1], list(list(fn = hub_genes_tool, params = list())))
desc <- dataset_description('human', 'CSF', 'myeloid', 'scRNA-seq')
synthesize_interpretation(packet, desc, mock_backend())
}
