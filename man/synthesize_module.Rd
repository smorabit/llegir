% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/orchestrator.R
\name{synthesize_module}
\alias{synthesize_module}
\title{Synthesize one evidence packet into a validated interpretation}
\usage{
synthesize_module(
  packet,
  desc,
  backend,
  temperature = 0,
  seed = NA_real_,
  prompt_template_version = PROMPT_TEMPLATE_VERSION,
  schema_path = system.file("schemas", "interpretation.schema.json", package = "llegir"),
  user_weights = list()
)
}
\arguments{
\item{packet}{An evidence packet, as built by \code{\link[=build_evidence_packet]{build_evidence_packet()}} /
\code{\link[=run_module]{run_module()}}.}

\item{desc}{A \code{dataset_description}; see \code{\link[=dataset_description]{dataset_description()}}.}

\item{backend}{A synthesis backend function; see \code{\link[=mock_backend]{mock_backend()}},
\code{\link[=ellmer_backend]{ellmer_backend()}}, \code{\link[=resolve_backend]{resolve_backend()}}.}

\item{temperature}{Sampling temperature passed to the backend.}

\item{seed}{Optional seed, recorded on the interpretation's provenance.}

\item{prompt_template_version}{Prompt template version to record on the
interpretation's provenance.}

\item{schema_path}{Path to the interpretation JSON schema; defaults to the
schema shipped with the package.}

\item{user_weights}{Named list of per-\code{tool_id} weight multipliers for
\code{\link[=calculate_fusion_score]{calculate_fusion_score()}}. Computed once here and shared between the
prompt's EVIDENCE CONFIDENCE MATRIX and \code{\link[=fuse_confidence]{fuse_confidence()}}, so the
printed fusion string can never drift from what the model was shown.
Default \code{list()}.}
}
\value{
A validated \code{interpretation} object.
}
\description{
Runs the full packet -> interpretation pipeline for one module: calls
\code{backend} via \code{\link[=synthesize_interpretation]{synthesize_interpretation()}}, enforces citation
faithfulness via \code{\link[=enforce_faithfulness]{enforce_faithfulness()}}, fuses model and deterministic
confidence via \code{\link[=fuse_confidence]{fuse_confidence()}}, and validates the result. These three
steps always run together -- an interpretation that skipped faithfulness
or fusion isn't one this engine should emit.
}
\examples{
ms <- llegir_example_moduleset()
packet <- run_module(ms, modules(ms)[1], list(list(fn = top_genes_tool, params = list())))
desc <- dataset_description('human', 'CSF', 'myeloid', 'scRNA-seq')
synthesize_module(packet, desc, mock_backend())
}
