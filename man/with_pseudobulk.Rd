% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/moduleset_pseudobulk.R
\name{with_pseudobulk}
\alias{with_pseudobulk}
\alias{capabilities.with_pseudobulk_ModuleSet}
\title{Attach a pseudo-bulk ModuleSet view to a cell-level ModuleSet}
\usage{
with_pseudobulk(cell_ms, pb_ms)

\method{capabilities}{with_pseudobulk_ModuleSet}(ms, ...)
}
\arguments{
\item{cell_ms}{A cell-level \code{ModuleSet} (e.g. built by \code{\link[=hdWGCNA_ModuleSet]{hdWGCNA_ModuleSet()}}).}

\item{pb_ms}{A pseudo-bulk \code{ModuleSet}, normally built by
\code{\link[=pseudobulk_ModuleSet]{pseudobulk_ModuleSet()}}.}

\item{ms}{A \code{ModuleSet} object; the dispatch target for
\code{\link[=capabilities.with_pseudobulk_ModuleSet]{capabilities.with_pseudobulk_ModuleSet()}}.}

\item{...}{Passed to methods.}
}
\value{
\code{cell_ms}, with \code{pb_ms} attached.
}
\description{
Stores \code{pb_ms} on \code{cell_ms} and flips \code{cell_ms}'s \code{pseudobulk} capability
to \code{TRUE}, so cell-level tools keep using \code{cell_ms} as their primary view
while pseudo-bulk-specific tools pull the sample-level view via
\code{\link[=pseudobulk]{pseudobulk()}} / \code{\link[=pseudobulk_view]{pseudobulk_view()}}. Implemented by prepending a
decorator class rather than rewriting every adapter: \code{capabilities()} and
\code{pseudobulk()} get an override, every other generic (\code{\link[=modules]{modules()}},
\code{\link[=gene_membership]{gene_membership()}}, \code{\link[=module_scores]{module_scores()}}, \code{\link[=expression]{expression()}}, \code{\link[=counts]{counts()}},
\code{\link[=metadata]{metadata()}}, \code{\link[=pkg_versions]{pkg_versions()}}) falls through unchanged to \code{cell_ms}'s own
method.
}
\examples{
\dontrun{
ms <- with_pseudobulk(cell_ms, pb_ms)
has_capability(ms, 'pseudobulk')
pseudobulk(ms)
}
}
