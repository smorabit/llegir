% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dataset_fragment.R
\name{write_dataset_figures}
\alias{write_dataset_figures}
\title{Materialize a dataset context's live plots to PNG files}
\usage{
write_dataset_figures(dataset_context, figures_dir)
}
\arguments{
\item{dataset_context}{A dataset context, as returned by
\code{\link[=build_dataset_context]{build_dataset_context()}} / \code{\link[=run_dataset_context]{run_dataset_context()}}.}

\item{figures_dir}{Output directory.}
}
\value{
\code{dataset_context}, with \code{image_path} filled in on every rendered
plot spec.
}
\description{
The dataset-level sibling of \code{\link[=write_fragment_figures]{write_fragment_figures()}}: renders each
\code{dataset_fragment}'s live \code{plot_obj} to
\verb{<figures_dir>/dataset/<fragment_id>__<plot_id>.png} and records that path
back onto the spec as \code{image_path}, so a report rendered after the live
\code{plot_obj} has been stripped can still embed the figure. Fragments with no
plots are skipped.
}
