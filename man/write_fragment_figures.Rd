% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fragment.R
\name{write_fragment_figures}
\alias{write_fragment_figures}
\title{Materialize every fragment's live plots to PNG files}
\usage{
write_fragment_figures(packet, figures_dir)
}
\arguments{
\item{packet}{An evidence packet, as returned by \code{\link[=build_evidence_packet]{build_evidence_packet()}}.}

\item{figures_dir}{Output directory.}
}
\value{
\code{packet}, with \code{image_path} filled in on every rendered plot spec.
}
\description{
The graphical sibling of \code{\link[=write_fragment_tables]{write_fragment_tables()}}: renders each fragment's
live \code{plot_obj} to \verb{<figures_dir>/<module_id>/<fragment_id>__<plot_id>.png}
and records that path back onto the spec as \code{image_path}, so a report
rendered from a re-read packet (where \code{plot_obj} has already been stripped;
see \code{\link[=attach_plots]{attach_plots()}}) can still embed the figure. A spec's own
\code{width}/\code{height}/\code{dpi} (see \code{\link[=attach_plots]{attach_plots()}}) are honored when set;
otherwise \code{\link[=save_plot_to_png]{save_plot_to_png()}}'s \code{7}/\code{4}/\code{150} defaults apply. Fragments
with no plots are skipped.
}
