% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exporters.R
\name{write_interpretation_report}
\alias{write_interpretation_report}
\title{Write a combined interpretation + evidence HTML report}
\usage{
write_interpretation_report(
  interps,
  packets,
  desc,
  dataset_context = NULL,
  output_file = "output/report.html",
  quiet = TRUE
)
}
\arguments{
\item{interps}{A named list of \code{interpretation} objects, keyed by module id
(e.g. the return value of \code{\link[=run_synthesis_orchestrator]{run_synthesis_orchestrator()}}).}

\item{packets}{A named list of evidence packets, keyed by module id (e.g. the
return value of \code{\link[=run_orchestrator]{run_orchestrator()}}).}

\item{desc}{The \code{dataset_description} used for this synthesis run.}

\item{dataset_context}{Optional dataset context (as built by
\code{\link[=build_dataset_context]{build_dataset_context()}} / \code{\link[=run_dataset_context]{run_dataset_context()}}). When supplied, its
\code{dataset_fragments} render in a "Dataset-level evidence" section above the
per-module sections -- each fragment's \code{compact_summary}, \code{top_findings}
table, and any attached \code{plots}. \code{NULL} (default) omits the section, and
the report is byte-identical to one produced without the argument.}

\item{output_file}{Destination HTML path. Default \code{'output/report.html'}.}

\item{quiet}{Suppress the pandoc/knitr progress output. Default \code{TRUE}.}
}
\value{
The rendered file path, invisibly.
}
\description{
Renders every module's synthesized interpretation alongside the raw
deterministic evidence it was drawn from into one human-readable HTML file.
Modules are paired by matching the names of \code{interps} and \code{packets}.
}
