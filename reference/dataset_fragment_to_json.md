# Serialize a dataset fragment to JSON

Serialize a dataset fragment to JSON

## Usage

``` r
dataset_fragment_to_json(frag, pretty = TRUE)
```

## Arguments

- frag:

  A `dataset_fragment` object.

- pretty:

  Pretty-print the JSON. Default `TRUE`.

## Value

A JSON string (a `jsonlite::json` scalar).
