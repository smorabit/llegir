# Serialize an interpretation to JSON

Serialize an interpretation to JSON

## Usage

``` r
interpretation_to_json(interp, pretty = TRUE)
```

## Arguments

- interp:

  An `interpretation` object.

- pretty:

  Pretty-print the JSON. Default `TRUE`.

## Value

A JSON string (a `jsonlite::json` scalar).
